name: midst

on:
  push:

jobs:
  passive:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        balloon: ["simplicity", "constant", "holiday", "especially", "branch"]
        gram: ["mean", "criminal", "instantly", "rabbit"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar mN.jar
